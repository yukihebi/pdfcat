THIRD-PARTY SOFTWARE NOTICES AND INFORMATION

This page lists the licenses of the projects used in pdfcat.

Overview of licenses:{{#each overview}}
- {{id}} ({{count}}){{/each}}


All license text:{{#each licenses}}
================================================================================
{{{name}}} ({{id}})

Used by:{{#each used_by}}
  - {{crate.name}} {{crate.version}} ({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}}){{/each}}
--------------------------------------------------------------------------------
{{{text}}}
{{/each}}
